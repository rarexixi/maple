server:
  port: 8080

spring:
  application:
    name: maple-api
  cache:
    type: redis
  redis:
    # cluster:
    #   nodes:
    #   max-redirects:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

feign:
  circuitbreaker:
    enabled: true
  okhttp:
    enabled: true